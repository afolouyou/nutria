<div class="app">
  <aside class="sidebar">
    <.link patch={~p"/chat"} class="new-chat-btn">
      <span class="plus">+</span> Nova conversa
    </.link>

    <div class="conversations-list">
      <%= for conv <- @conversations || [] do %>
        <.link
          patch={~p"/chat/#{conv["id"]}"}
          class={[
            "conversation-item",
            if(@current_conversation_id == conv["id"], do: "active")
          ]}
        >
          <%= conv["title"] %>
        </.link>
      <% end %>

      <%= if @conversations == [] || @conversations == nil do %>
        <p class="no-conversations">Nenhuma conversa ainda</p>
      <% end %>
    </div>

    <div class="sidebar-footer">
      <.link navigate={~p"/pantry"} class="pantry-link">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <rect x="3" y="3" width="18" height="18" rx="2"/>
          <path d="M3 9h18M9 3v18"/>
        </svg>
        Despensa
      </.link>

      <%= if @current_user do %>
        <div class="profile-wrapper" id="profile-dropdown" phx-hook="ProfileDropdown">
          <button class="user-profile">
            <div class="avatar">
              <%= if @current_user.avatar do %>
                <img src={"/uploads/avatars/" <> @current_user.avatar} alt="Foto de perfil" class="avatar-photo" />
              <% else %>
                <%= String.first(@current_user.name || "?") %>
              <% end %>
            </div>
            <span class="user-name"><%= @current_user.name %></span>
            <span class="dropdown-arrow">&#9662;</span>
          </button>

          <div class="dropdown-menu" id="profile-dropdown-menu">
            <button class="dropdown-item" onclick="document.getElementById('settings-dialog').showModal()">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" width="16" height="16">
                <circle cx="12" cy="12" r="3"/>
                <path d="M12 1v2M12 21v2M4.22 4.22l1.42 1.42M18.36 18.36l1.42 1.42M1 12h2M21 12h2M4.22 19.78l1.42-1.42M18.36 5.64l1.42-1.42"/>
              </svg>
              Configurações
            </button>
            <div class="dropdown-separator"></div>
            <.link href={~p"/session/destroy"} method="delete" class="dropdown-item dropdown-item-danger">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" width="16" height="16">
                <path d="M9 21H5a2 2 0 01-2-2V5a2 2 0 012-2h4"/>
                <polyline points="16 17 21 12 16 7"/>
                <line x1="21" y1="12" x2="9" y2="12"/>
              </svg>
              Sair da conta
            </.link>
          </div>
        </div>
      <% end %>
    </div>
  </aside>

  <main class="main">
    <.flash_group flash={@flash} />
    {@inner_content}
  </main>
</div>

<%= if @current_user do %>
  <%= live_render(@socket, NutriaWeb.SettingsDialogLive,
    id: "settings-dialog-live",
    session: %{"current_user_id" => @current_user.id}
  ) %>
<% end %>
